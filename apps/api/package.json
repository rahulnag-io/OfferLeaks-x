{
  "name": "@offerleaks/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn offerleaks.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "uv sync --frozen",
    "lint": "uv run ruff check . && uv run mypy src",
    "test": "uv run pytest",
    "type-check": "uv run mypy src"
  }
}
