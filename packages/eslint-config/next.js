// Reconstructed stub (see package.json note) -- the flat-config array
// ESLint 9 expects. Kept intentionally permissive; the real
// `@offerleaks/eslint-config` package wasn't part of this project
// backup, only `apps/*`.
export default [];
